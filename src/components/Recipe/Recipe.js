import React from "react";
import RecipeItem from "./RecipeItem/RecipeItem"

export const Recipe = () => {
    return (
        <section id="recipe-page">
            <h1>All Recipes</h1>

            <h3 className="no-recipe">No recipe yet</h3>

        </section>
    );
};